/**
 * Demo team shown when no team id is saved yet.
 *
 * Kept in one place so the two pages that reference it cannot drift — the
 * placeholder text and the actual default were previously separate literals.
 */
export const DEMO_TEAM_ID = '8648898'
